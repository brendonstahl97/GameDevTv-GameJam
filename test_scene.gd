extends Node3D

@onready var training_customer: Area3D = %"Training Customer"

# Called when the node enters the scene tree for the first time.
#func _ready() -> void:
	#SignalBus.display_dialog.emit("intro")
